{
  "framework": null,
  "installCommand": "npm install --prefix client && npm install --prefix server && npm run prisma:generate --prefix server",
  "buildCommand": "npm run build --prefix client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
